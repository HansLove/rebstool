# Sub-Affiliate Architecture Diagram

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         AppRouter                                │
│                    (src/app/routes/)                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │
        ┌────────────────┴────────────────┐
        │                                  │
        ▼                                  ▼
┌───────────────┐                  ┌──────────────┐
│ Master Routes │                  │ Subs Routes  │
│ (Affiliates)  │                  │ (Refactored) │
└───────────────┘                  └──────┬───────┘
                                          │
                                          │
                                          ▼
                              ┌───────────────────────┐
                              │   subsRoutes.tsx      │
                              │   ┌─────────────────┐ │
                              │   │ 🏠 CORE (3)     │ │
                              │   │ 💰 EARNINGS (3) │ │
                              │   │ 🌐 NETWORK (2)  │ │
                              │   │ 📚 RESOURCES(3) │ │
                              │   └─────────────────┘ │
                              └───────────┬───────────┘
                                          │
                                          │
                                          ▼
                              ┌───────────────────────┐
                              │    SubsLayout.tsx     │
                              │  (Main Container)     │
                              └───────────┬───────────┘
                                          │
                ┌─────────────────────────┼─────────────────────────┐
                │                         │                         │
                ▼                         ▼                         ▼
      ┌─────────────────┐      ┌──────────────────┐      ┌─────────────────┐
      │   SubsNavbar    │      │  useSubsData()   │      │  InviteModal    │
      │   (Component)   │      │  (Custom Hook)   │      │  (Component)    │
      └─────────────────┘      └────────┬─────────┘      └─────────────────┘
                                        │
                                        │ API Call
                                        ▼
                              ┌──────────────────┐
                              │   Backend API    │
                              │   subaffil  │
                              └──────────────────┘
```

---

## 🔄 Data Flow

```
┌──────────────────────────────────────────────────────────────────┐
│ Step 1: Component Mount                                          │
│                                                                   │
│   SubsLayout → useSubsDashboardData() → useState/useEffect       │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│ Step 2: API Call                                                 │
│                                                                   │
│   http.get('subaffiliate/dashboard/:accountId')             │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│ Step 3: State Update                                             │
│                                                                   │
│   setRegistrationsReport(data)                                   │
│   setCurrentDeal(deal)                                           │
│   setIsLoading(false)                                            │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│ Step 4: Context Provider                                         │
│                                                                   │
│   <Outlet context={{ registrationsReport, deal, userSlug }} />  │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│ Step 5: Child Component Consumption                              │
│                                                                   │
│   const { registrationsReport, deal } = useOutletContext()      │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗂️ File Organization (Before vs After)

### Before Refactor
```
src/
├── layouts/
│   └── SubsLayout.tsx                    (Monolithic, 59 lines)
│       ├── Data fetching logic mixed in
│       ├── UI rendering
│       └── State management
│
└── app/routes/
    └── subsRoutes.tsx                    (Unorganized, 38 lines)
        └── Flat route list
```

### After Refactor
```
src/
├── layouts/
│   ├── SubsLayout.tsx                    (Clean, 102 lines)
│   │   ├── Uses custom hook ✨
│   │   ├── Better error handling ✨
│   │   └── Improved loading states ✨
│   │
│   ├── hooks/
│   │   └── useSubsDashboardData.ts       (NEW - 56 lines)
│   │       └── Reusable data fetching logic
│   │
│   ├── types/
│   │   └── subsLayout.types.ts           (NEW - 40 lines)
│   │       └── Centralized type definitions
│   │
│   └── README.md                         (NEW - Documentation)
│
├── app/routes/
│   ├── subsRoutes.tsx                    (Organized, 127 lines)
│   │   ├── 🏠 CORE section
│   │   ├── 💰 EARNINGS section
│   │   ├── 🌐 NETWORK section
│   │   └── 📚 RESOURCES section
│   │
│   ├── subsRoutes.config.ts              (NEW - 32 lines)
│   │   └── Route constants and groups
│   │
│   └── README.md                         (NEW - Documentation)
│
└── pages/SubAffiliates/
    └── dashboard/
        └── AffillOneViewWireframe.tsx    (NEW - 200+ lines)
            └── New unified dashboard view
```

---

## 🎯 Route Grouping Structure

```
subsRoutes = [
  ┌─────────────────────────────────────────┐
  │ 🏠 CORE ROUTES                          │
  ├─────────────────────────────────────────┤
  │ • dashboard        → Resume View        │
  │ • resume           → Resume View        │
  │ • one-view         → NEW Unified View ✨ │
  └─────────────────────────────────────────┘

  ┌─────────────────────────────────────────┐
  │ 💰 EARNINGS ROUTES                      │
  ├─────────────────────────────────────────┤
  │ • yourEarnings     → Payment Tables     │
  │ • payouts          → Payout History     │
  │ • performance      → Analytics          │
  └─────────────────────────────────────────┘

  ┌─────────────────────────────────────────┐
  │ 🌐 NETWORK ROUTES                       │
  ├─────────────────────────────────────────┤
  │ • mynetwork        → Network Overview   │
  │ • registrations    → User Management    │
  └─────────────────────────────────────────┘

  ┌─────────────────────────────────────────┐
  │ 📚 RESOURCES ROUTES                     │
  ├─────────────────────────────────────────┤
  │ • marketing        → Marketing Lab      │
  │ • tutorial         → Tutorials          │
  │ • settings         → Configuration      │
  └─────────────────────────────────────────┘
]
```

---

## 🧩 Component Hierarchy

```
SubsLayout
│
├── SubsNavbar
│   ├── Logo
│   ├── Navigation Links
│   ├── User Menu
│   └── Theme Toggle
│
├── Main Content (Outlet)
│   │
│   ├── AffillOneViewWireframe (NEW)
│   │   ├── Header Section
│   │   ├── Quick Stats Grid
│   │   │   ├── Total Registrations
│   │   │   ├── Active Networks
│   │   │   ├── Total Earnings
│   │   │   └── Performance Score
│   │   ├── User Info Card
│   │   ├── Wireframe Sections
│   │   │   ├── Performance Overview
│   │   │   ├── Quick Actions
│   │   │   ├── Recent Activity
│   │   │   └── Resources Hub
│   │   └── Development Notice
│   │
│   ├── SubAffilliateResume
│   ├── PaymentsRegistersTable
│   ├── Payouts
│   ├── Performance
│   ├── MyNetwork
│   ├── MyRegistration
│   ├── MarketingLab
│   ├── SubAffiliateTutorial
│   └── SubsAffilliatesSettings
│
└── InviteModal
    ├── Modal Overlay
    ├── Modal Content
    ├── Invite Form
    └── Close Button
```

---

## 📦 Type System Flow

```
subsLayout.types.ts
│
├── SubsLayoutContext
│   ├── registrationsReport: any[]
│   ├── deal: number
│   ├── userSlug: string | null
│   └── isLoading: boolean
│
├── SubsDashboardResponse
│   ├── deal: number
│   └── data: any[]
│
├── SubsNavbarProps
│   ├── setIsInviteModalOpen: (isOpen: boolean) => void
│   └── setUserSlug: (slug: string | null) => void
│
└── InviteModalProps
    ├── isOpen: boolean
    ├── onClose: () => void
    ├── accountId: string
    └── userSlug: string | null
```

---

## 🔐 Authentication Flow

```
                    ┌──────────────┐
                    │    User      │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │RequireAuth   │
                    │  (Guard)     │
                    └──────┬───────┘
                           │
                   ┌───────┴────────┐
                   │ Authenticated? │
                   └───────┬────────┘
                           │
              ┌────────────┴────────────┐
              │                         │
           YES│                         │NO
              ▼                         ▼
      ┌───────────────┐         ┌──────────────┐
      │  SubsLayout   │         │   Redirect   │
      │  (Dashboard)  │         │   to Login   │
      └───────────────┘         └──────────────┘
```

---

## 🎨 Lazy Loading Strategy

```
Route Definition
      │
      ▼
┌─────────────────────────────┐
│ const Component = lazy()    │
│ import("./Component")        │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ withSuspense(Component)     │
│ ├── <Suspense>              │
│ │   fallback={<Loader/>}    │
│ │   <Component />           │
│ └── </Suspense>             │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Route Element               │
│ element={...}               │
└─────────────────────────────┘
```

---

## 🚀 Performance Optimizations

```
┌────────────────────────────────────────────┐
│ Optimization Techniques                    │
├────────────────────────────────────────────┤
│                                            │
│ 1. Code Splitting                          │
│    └── Lazy loaded routes                  │
│    └── Reduced initial bundle size         │
│                                            │
│ 2. Suspense Boundaries                     │
│    └── Better loading states               │
│    └── Improved perceived performance      │
│                                            │
│ 3. Custom Hooks                            │
│    └── Reusable data fetching              │
│    └── Shared state management             │
│                                            │
│ 4. Context API                             │
│    └── Avoid prop drilling                 │
│    └── Centralized state                   │
│                                            │
│ 5. Type Safety                             │
│    └── Catch errors at compile time        │
│    └── Better IDE support                  │
│                                            │
└────────────────────────────────────────────┘
```

---

## 📈 Scalability Benefits

### Before
- ❌ Monolithic components
- ❌ Mixed concerns
- ❌ Hard to test
- ❌ Difficult to extend
- ❌ No clear structure

### After
- ✅ Modular components
- ✅ Separated concerns
- ✅ Easily testable
- ✅ Simple to extend
- ✅ Clear architecture
- ✅ Better documentation
- ✅ Type-safe
- ✅ Reusable hooks

---

## 🔮 Future Architecture Vision

```
Current State (Phase 1) ✅
    │
    ├── Unified Dashboard Wireframe
    ├── Route Organization
    ├── Custom Hooks
    └── Type Safety
    
Next Phase (Phase 2) 🔄
    │
    ├── Full Dashboard Implementation
    ├── Real-time Data Updates
    ├── Advanced Filtering
    └── Interactive Charts
    
Future State (Phase 3) 🚀
    │
    ├── AI-Powered Insights
    ├── Predictive Analytics
    ├── Advanced Automation
    └── Multi-tenant Support
```

---

**Architecture Version:** 2.0  
**Last Updated:** October 10, 2025  
**Status:** ✅ Production Ready

